# do not modify these
root: ./
name: simulation
socket_name: mrs
attach: false
tmux_options: -f /etc/ctu-mrs/tmux.conf
# you can modify these
startup_window: map_plan
pre_window:
  - export UAV_NAME=uav1
  - export USE_SIM_TIME="true"
  - export UAV_TYPE="x500"
  - export RMW_IMPLEMENTATION=rmw_zenoh_cpp
  - export RES_PKG=`ros2 pkg prefix biomass_simulation_resources`/share/biomass_simulation_resources
  - export GZ_SIM_RESOURCE_PATH=$RES_PKG:$RES_PKG/models:$RES_PKG/models/forest-gen-models:$RES_PKG/worlds
  - export PLATFORM_CONFIG=`ros2 pkg prefix mrs_uav_gazebo_simulator`/share/mrs_uav_gazebo_simulator/config/mrs_uav_system/$UAV_TYPE.yaml
  - export WORLD_FILE=$RES_PKG/worlds/mrs-small-forest-test.world
windows:
  - router:
      layout: tiled
      panes:
        - ros2 run rmw_zenoh_cpp rmw_zenohd
  - gazebo:
      layout: tiled
      panes:
        - ros2 launch mrs_uav_gazebo_simulator simulation.launch.py gz_headless:=false gz_verbose:=true world_file:=$WORLD_FILE
        - |
          waitForGazebo; sleep 5; ros2 service call /mrs_drone_spawner/spawn mrs_msgs/srv/String "{value: '1 --$UAV_TYPE --enable-rangefinder --enable-ouster --enable-ground-truth model:=OS0-128 use_gpu:=True update_rate:=10 --pos 0 0 1.0 0'}"
        - ros2 launch mrs_uav_px4_api api.launch.py
  - core:
      layout: tiled
      panes:
        - ros2 launch mrs_uav_core core.launch.py
          platform_config:=$PLATFORM_CONFIG
          world_config:=./config/world_config.yaml
          custom_config:=./config/custom_config.yaml
          network_config:=./config/network_config.yaml
  - status:
      layout: tiled
      panes:
        - ros2 run mrs_uav_status status.sh
  - map_plan:
      layout: tiled
      panes:
        - ros2 launch mrs_octomap_mapping_planning mapplan.launch.py custom_config:=./config/mapplan_config.yaml lidar_3d_0:=ouster/points
  - takeoff:
      layout: tiled
      panes:
        - ros2 launch mrs_uav_autostart automatic_start.launch.py custom_config:=./config/automatic_start.yaml
        - waitForCore; sleep 10; ./takeoff.sh
  - goto:
      layout: tiled
      panes:
        - 'history -s ros2 service call /$UAV_NAME/control_manager/goto mrs_msgs/srv/Vec4 "{goal: [1.5, 1.5, 1.0, 0.0]}"'
  - rviz:
      layout: tiled
      panes:
        - ros2 run rviz2 rviz2 -d ./rviz.rviz --ros-args -p use_sim_time:=true
  - layout:
      layout: tiled
      panes:
        - sleep 5; ~/.i3/layout_manager.sh ./layout.json
